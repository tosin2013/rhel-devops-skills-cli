# VP Submission — Research Questions

Open research items extracted from `SKILL.md`. Resolve via `skill-researcher`.

| RQ | Description | Primary source |
|----|-------------|----------------|
| VP-SUB-1 | Exact required fields for `pattern-metadata.yaml` at Community tier — field names, types, and which are mandatory vs. optional per validatedpatterns.io current schema | validatedpatterns/docs `content/` and validatedpatterns/common |
| VP-SUB-2 | CI/CD requirements for Tested tier — accepted platforms, convergence check spec, OCP version matrix. Also: whether the VP team has an official CI template or GitHub Actions reusable workflow (check validatedpatterns/docs/.github/ and validatedpatterns/common) | validatedpatterns/docs `AGENTS.md`, `.github/workflows/`, VP team documentation |
| VP-SUB-3 | Maintained tier requirements — SLA values, what qualifies as "Red Hat involvement", whether RHDP team ownership counts, and how the VP team verifies maintainer activity | validatedpatterns/docs contributor guidelines |
| VP-SUB-4 | validatedpatterns/docs content structure — directory layout, Hugo frontmatter schema, tier badge syntax, required fields (check `archetypes/` and existing pattern pages in `content/patterns/`) | validatedpatterns/docs `archetypes/`, `content/patterns/` examples |
| VP-SUB-5 | PR reviewer expectations and merge criteria — OWNERS file, CI results, tier-specific requirements, response time SLA, and whether a demo or recording is required | validatedpatterns/docs CONTRIBUTING.md or OWNERS file |
